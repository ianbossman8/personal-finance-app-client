sudo: required
services:
  - docker

before_install:
  - docker build -t client/personal-finance-app-client -f Dockerfile.dev .

script:
  - docker run -e CI=true client/personal-finance-app-client npm run test

after_success:
  - docker build -t ian035/personal-finance-app-client .
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push ian035/personal-finance-app-client

deploy:
  edge: true
  provider: elasticbeanstalk
  region: 'eu-west-2'
  app: 'personal-finance-app-client'
  env: 'PersonalFinanceAppClient-env'
  bucket_name: 'elasticbeanstalk-eu-west-2-406965596077'
  bucket_path: 'personal-finance-app-client'
  on:
    branch: master
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key: $AWS_SECRET_KEY
